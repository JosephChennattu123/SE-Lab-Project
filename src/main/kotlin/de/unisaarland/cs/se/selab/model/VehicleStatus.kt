package de.unisaarland.cs.se.selab.model

/** enum class for the different vehicle states */
enum class VehicleStatus {
    AT_BASE, ASSIGNED, WAITING, HANDLING, UNAVAILABLE, TO_EMERGENCY, RETURNING, BUSY, NEEDS_SERVICE
}
